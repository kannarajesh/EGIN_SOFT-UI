apiVersion: v1
kind: Service
metadata:
  name: eginsoftui
  labels:
    app: eginsoftui
spec:
  type: NodePort
  selector:
    app: eginsoftui
  ports:
    - port: 8000
      nodePort: 30011
      protocol: TCP
      targetPort: 8000
